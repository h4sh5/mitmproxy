import * as React from "react";

ReplayerMenu.title = "Replayer";

export default function ReplayerMenu() {
    //empty component since we don't have an header menu for capture, but we just display the title
    return <></>;
}
